<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.korit.clovapi.domain.plan.mapper.PlanMapper"><resultMap id="p" type="com.korit.clovapi.domain.plan.entity.Plan"><id property="id" column="id"/><result property="roomId" column="room_id"/><result property="writerId" column="writer_id"/><result property="writerNickname" column="nickname"/><result property="writerProfileImageUrl" column="profile_image_url"/><result property="title" column="title"/><result property="planDate" column="plan_date"/><result property="description" column="description"/><result property="status" column="status"/><result property="memoryStatus" column="memory_status"/><result property="completedAt" column="completed_at"/><result property="createdAt" column="created_at"/></resultMap><sql id="cols">p.id,p.room_id,p.writer_id,u.nickname,u.profile_image_url,p.title,p.plan_date,p.description,p.status,p.memory_status,p.completed_at,p.created_at</sql><insert id="insert" useGeneratedKeys="true" keyProperty="id">INSERT INTO plans(room_id,writer_id,title,plan_date,description) VALUES(#{roomId},#{writerId},#{title},#{planDate},#{description})</insert><select id="findById" resultMap="p">SELECT <include refid="cols"/> FROM plans p JOIN users u ON u.id=p.writer_id WHERE p.id=#{planId}</select><select id="findByRoomId" resultMap="p">SELECT <include refid="cols"/> FROM plans p JOIN users u ON u.id=p.writer_id WHERE p.room_id=#{roomId}<if test="status!=null"> AND p.status=#{status}</if><if test="from!=null"> AND p.plan_date&gt;=#{from}</if><if test="to!=null"> AND p.plan_date&lt;=#{to}</if> ORDER BY p.plan_date,p.id</select><update id="updateByIdAndWriterId">UPDATE plans <set><if test="request.title!=null">title=#{request.title},</if><if test="request.planDate!=null">plan_date=#{request.planDate},</if><if test="request.description!=null">description=#{request.description},</if></set> WHERE id=#{planId} AND writer_id=#{writerId}</update><delete id="deleteByIdAndWriterId">DELETE FROM plans WHERE id=#{planId} AND writer_id=#{writerId}</delete><update id="complete">UPDATE plans SET status='COMPLETED',memory_status='CANDIDATE',completed_at=#{completedAt},memory_candidate_created_at=#{completedAt} WHERE id=#{planId}</update><update id="cancelByIdAndWriterId">UPDATE plans SET status='CANCELED' WHERE id=#{planId} AND writer_id=#{writerId}</update><update id="skipMemory">UPDATE plans SET memory_status='SKIPPED' WHERE id=#{planId}</update></mapper>
