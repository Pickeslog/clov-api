<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.korit.clovapi.domain.plan.mapper.ChecklistMapper"><resultMap id="c" type="com.korit.clovapi.domain.plan.entity.PlanChecklist"><id property="id" column="id"/><result property="planId" column="plan_id"/><result property="content" column="content"/><result property="checked" column="checked"/></resultMap><insert id="insert" useGeneratedKeys="true" keyProperty="id">INSERT INTO plan_checklists(plan_id,content) VALUES(#{planId},#{content})</insert><select id="findByPlanId" resultMap="c">SELECT id,plan_id,content,checked FROM plan_checklists WHERE plan_id=#{planId} ORDER BY id</select><select id="findById" resultMap="c">SELECT id,plan_id,content,checked FROM plan_checklists WHERE id=#{checklistId}</select><update id="update">UPDATE plan_checklists <set><if test="request.content!=null">content=#{request.content},</if><if test="request.checked!=null">checked=#{request.checked},</if></set> WHERE id=#{checklistId}</update><delete id="deleteById">DELETE FROM plan_checklists WHERE id=#{checklistId}</delete></mapper>
