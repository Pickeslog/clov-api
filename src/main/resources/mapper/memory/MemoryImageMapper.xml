<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.korit.clovapi.domain.memory.mapper.MemoryImageMapper">

    <resultMap id="memoryImageResultMap" type="com.korit.clovapi.domain.memory.entity.MemoryImage">
        <id property="id" column="id"/>
        <result property="memoryId" column="memory_id"/>
        <result property="imageUrl" column="image_url"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO memory_images (memory_id, image_url, sort_order)
        VALUES (#{memoryId}, #{imageUrl}, #{sortOrder})
    </insert>

    <select id="findById" resultMap="memoryImageResultMap">
        SELECT id, memory_id, image_url, sort_order, created_at
        FROM memory_images
        WHERE id = #{imageId}
    </select>

    <select id="countByMemoryId" resultType="int">
        SELECT COUNT(*) FROM memory_images WHERE memory_id = #{memoryId}
    </select>

    <select id="findByMemoryId" resultMap="memoryImageResultMap">
        SELECT id, memory_id, image_url, sort_order, created_at
        FROM memory_images
        WHERE memory_id = #{memoryId}
        ORDER BY sort_order ASC, id ASC
    </select>

    <delete id="deleteById">
        DELETE FROM memory_images WHERE id = #{imageId}
    </delete>

    <update id="updateSortOrder">
        UPDATE memory_images SET sort_order = #{sortOrder} WHERE id = #{imageId}
    </update>
</mapper>
