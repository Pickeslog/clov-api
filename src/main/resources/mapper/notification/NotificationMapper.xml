<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.korit.clovapi.domain.notification.mapper.NotificationMapper">

    <resultMap id="notificationResultMap" type="com.korit.clovapi.domain.notification.entity.Notification">
        <id property="id" column="id"/>
        <result property="roomId" column="room_id"/>
        <result property="recipientId" column="recipient_id"/>
        <result property="actorId" column="actor_id"/>
        <result property="actorNickname" column="actor_nickname"/>
        <result property="actorProfileImageUrl" column="actor_profile_image_url"/>
        <result property="type" column="type"/>
        <result property="referenceId" column="reference_id"/>
        <result property="isRead" column="is_read"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="getNotifications" resultMap="notificationResultMap">
        SELECT n.id, n.room_id, n.recipient_id, n.actor_id, u.nickname AS actor_nickname, u.profile_image_url AS actor_profile_image_url, n.type, n.reference_id, n.is_read, n.created_at
        FROM notifications n
        LEFT JOIN users u ON u.id = n.actor_id
        WHERE n.room_id = #{roomId}
          AND n.recipient_id = #{recipientId}
        <if test="type != null">
          AND n.type = #{type}
        </if>
        ORDER BY n.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="findById" resultMap="notificationResultMap">
        SELECT n.id, n.room_id, n.recipient_id, n.actor_id, u.nickname AS actor_nickname, u.profile_image_url AS actor_profile_image_url, n.type, n.reference_id, n.is_read, n.created_at
        FROM notifications n
        LEFT JOIN users u ON u.id = n.actor_id
        WHERE n.id = #{id}
    </select>

    <update id="markAsRead">
        UPDATE notifications
        SET is_read = true
        WHERE id = #{id}
    </update>

    <update id="markAllAsRead">
        UPDATE notifications
        SET is_read = true
        WHERE room_id = #{roomId}
          AND recipient_id = #{recipientId}
          AND is_read = false
    </update>


</mapper>
