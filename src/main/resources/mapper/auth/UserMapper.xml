<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.korit.clovapi.domain.auth.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.korit.clovapi.domain.auth.entity.User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="oauthProvider" column="oauth_provider"/>
        <result property="oauthSubject" column="oauth_subject"/>
        <result property="nickname" column="nickname"/>
        <result property="profileImageUrl" column="profile_image_url"/>
        <result property="birthdate" column="birthdate"/>
        <result property="termsAgreedAt" column="terms_agreed_at"/>
        <result property="privacyAgreedAt" column="privacy_agreed_at"/>
        <result property="marketingAgreedAt" column="marketing_agreed_at"/>
        <result property="anonymized" column="is_anonymized"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByEmail" resultMap="userResultMap">
        SELECT id, email, password, oauth_provider, oauth_subject, nickname, profile_image_url, birthdate,
               terms_agreed_at, privacy_agreed_at, marketing_agreed_at, is_anonymized, created_at
        FROM users
        WHERE email = #{email}
    </select>

    <select id="findById" resultMap="userResultMap">
        SELECT id, email, password, oauth_provider, oauth_subject, nickname, profile_image_url, birthdate,
               terms_agreed_at, privacy_agreed_at, marketing_agreed_at, is_anonymized, created_at
        FROM users
        WHERE id = #{id}
    </select>

    <select id="findByOauth" resultMap="userResultMap">
        SELECT id, email, password, oauth_provider, oauth_subject, nickname, profile_image_url, birthdate,
               terms_agreed_at, privacy_agreed_at, marketing_agreed_at, is_anonymized, created_at
        FROM users
        WHERE oauth_provider = #{oauthProvider}
          AND oauth_subject = #{oauthSubject}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (
            email, password, oauth_provider, oauth_subject, nickname, profile_image_url, birthdate,
            terms_agreed_at, privacy_agreed_at, marketing_agreed_at
        ) VALUES (
            #{email}, #{password}, #{oauthProvider}, #{oauthSubject}, #{nickname}, #{profileImageUrl}, #{birthdate},
            #{termsAgreedAt}, #{privacyAgreedAt}, #{marketingAgreedAt}
        )
    </insert>
    <update id="updateProfile">
        UPDATE users
        <set>
            <if test="request.nickname != null">nickname = #{request.nickname},</if>
            <if test="request.profileImageUrl != null">profile_image_url = #{request.profileImageUrl},</if>
            <if test="request.birthdate != null">birthdate = #{request.birthdate},</if>
        </set>
        WHERE id = #{userId}
    </update>
    <update id="updatePassword">
        UPDATE users SET password = #{password} WHERE id = #{userId}
    </update>
    <!-- #157 — email까지 비워야 두 가지가 동시에 풀린다: ①로그인 차단(login()이 findByEmail로
         찾으므로 이 컬럼이 바뀌면 자연히 못 찾는다) ②같은 이메일로 재가입 허용(uk_users_email이
         유일키라 원래 값을 그대로 두면 영원히 못 쓴다). id를 붙여 유일성을 보장하고 .invalid는
         RFC 2606 예약 도메인(실제로 존재하거나 발송 가능한 주소가 아님이 보장됨)이라 골랐다.
         password·oauth_*도 같이 비워야 소셜 계정 재가입도 같은 방식으로 자연히 풀린다
         (uk_users_oauth 복합 유니크는 NULL끼리 서로 다른 값 취급이라 충돌 없음). -->
    <update id="anonymize">
        UPDATE users SET
            is_anonymized = true,
            nickname = '언노운',
            anonymized_at = CURRENT_TIMESTAMP,
            email = CONCAT('anonymized-', id, '@clov.invalid'),
            password = NULL,
            oauth_provider = NULL,
            oauth_subject = NULL
        WHERE id = #{userId}
    </update>

    <!-- #159 — EXISTS로 감싸서 행이 아예 없을 때도(위조된 id 등) null이 아니라 항상 false를
         반환한다(ShopMapper.existsInInventory와 같은 관례). -->
    <select id="isAnonymized" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM users WHERE id = #{id} AND is_anonymized = true)
    </select>
</mapper>
