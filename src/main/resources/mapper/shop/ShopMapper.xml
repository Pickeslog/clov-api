<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.korit.clovapi.domain.shop.mapper.ShopMapper">

    <resultMap id="shopItemResultMap" type="com.korit.clovapi.domain.shop.entity.ShopItem">
        <id property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="category" column="category"/>
        <result property="rarity" column="rarity"/>
        <result property="price" column="price"/>
        <result property="discountRate" column="discount_rate"/>
        <result property="imageUrl" column="image_url"/>
        <result property="status" column="status"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="owned" column="owned"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <resultMap id="walletResultMap" type="com.korit.clovapi.domain.shop.entity.UserWallet">
        <id property="userId" column="user_id"/>
        <result property="balance" column="balance"/>
    </resultMap>

    <select id="findCatalog" resultMap="shopItemResultMap">
        SELECT i.id, i.code, i.name, i.description, i.category, i.rarity, i.price, i.discount_rate,
               i.image_url, i.status, i.sort_order, i.created_at, i.updated_at,
               (inv.id IS NOT NULL) AS owned
        FROM shop_items i
        LEFT JOIN user_inventory_items inv ON inv.item_id = i.id AND inv.user_id = #{userId}
        <!--
          RETIRED는 목록에서 뺀다. 이 조건이 없으면 내린 상품이 그대로 보이고, 누르면
          ITEM_NOT_PURCHASABLE로 떨어져 "보이는데 못 사는" 상태가 된다(계약 §15-1).
          내린 상품을 이미 산 사람은 findInventory로 계속 보므로 소유·장착에는 영향이 없다.
        -->
        WHERE i.status = 'ACTIVE'
          AND (#{category} IS NULL OR i.category = #{category})
          AND (#{rarity} IS NULL OR i.rarity = #{rarity})
        ORDER BY i.sort_order ASC, i.id ASC
    </select>

    <select id="findInventory" resultMap="shopItemResultMap">
        SELECT i.id, i.code, i.name, i.description, i.category, i.rarity, i.price, i.discount_rate,
               i.image_url, i.status, i.sort_order, i.created_at, i.updated_at, TRUE AS owned
        FROM user_inventory_items inv
        JOIN shop_items i ON i.id = inv.item_id
        WHERE inv.user_id = #{userId}
        ORDER BY inv.purchased_at DESC
    </select>

    <select id="findById" resultMap="shopItemResultMap">
        SELECT id, code, name, description, category, rarity, price, discount_rate,
               image_url, status, sort_order, created_at, updated_at, FALSE AS owned
        FROM shop_items
        WHERE id = #{itemId}
    </select>

    <!-- 구매는 읽고-확인-차감이라 동시 구매 시 잔액이 꼬일 수 있어 행을 잠그고 읽는다. -->
    <select id="findWalletForUpdate" resultMap="walletResultMap">
        SELECT user_id, balance FROM user_wallets WHERE user_id = #{userId} FOR UPDATE
    </select>

    <select id="findWallet" resultMap="walletResultMap">
        SELECT user_id, balance FROM user_wallets WHERE user_id = #{userId}
    </select>

    <!-- IGNORE: 동시 요청으로 지갑 존재 확인 사이에 경합이 생겨도 중복 생성 에러 대신 조용히 넘어간다. -->
    <insert id="insertWallet">
        INSERT IGNORE INTO user_wallets (user_id, balance) VALUES (#{userId}, #{balance})
    </insert>

    <update id="updateBalance">
        UPDATE user_wallets SET balance = #{balance} WHERE user_id = #{userId}
    </update>

    <select id="existsInInventory" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM user_inventory_items WHERE user_id = #{userId} AND item_id = #{itemId})
    </select>

    <insert id="insertInventory">
        INSERT INTO user_inventory_items (user_id, item_id, paid_price) VALUES (#{userId}, #{itemId}, #{paidPrice})
    </insert>

    <insert id="insertTransaction">
        INSERT INTO wallet_transactions (user_id, reason, amount, balance_after, reference_id)
        VALUES (#{userId}, #{reason}, #{amount}, #{balanceAfter}, #{referenceId})
    </insert>

    <!-- 계약 §15-4 — 합산 대상은 반드시 EARN_ 접두사 전체다. 사유를 이름으로 나열하면
         새 획득 사유가 조용히 캡 밖으로 빠지고, 반대로 ADMIN_GRANT(운영 지급) 같은
         비획득 사유를 얹으면 지급받은 날 정상 획득이 통째로 막힌다.

         ★ 이스케이프 문자로 백슬래시를 쓰지 말 것. MySQL은 문자열 리터럴 안에서 백슬래시를
         이스케이프 문자로 먼저 해석하므로 ESCAPE '\' 는 닫는 따옴표가 이스케이프돼
         구문 오류가 된다(2026-08-05 CI에서 실제로 났다 — 이 쿼리가 깨지면서 earnGold 를
         타는 모든 경로가 500이 됐다). '!' 는 그런 이중 해석이 없다.

         ESCAPE 자체는 반드시 있어야 한다 — 없으면 '_' 가 임의의 한 글자로 매칭돼
         EARNX_... 같은 사유까지 합산에 걸린다. -->
    <select id="sumEarnedToday" resultType="long">
        SELECT COALESCE(SUM(amount), 0) FROM wallet_transactions
        WHERE user_id = #{userId} AND reason LIKE 'EARN!_%' ESCAPE '!' AND created_at >= #{since}
    </select>

    <select id="countReasonToday" resultType="int">
        SELECT COUNT(*) FROM wallet_transactions
        WHERE user_id = #{userId} AND reason = #{reason} AND created_at >= #{since}
    </select>
</mapper>
